<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../styles/output.css" />
    <title>Membership Form</title>
  </head>
  <body class="bg-gray-100 font-sans">
    <%- include('headers.ejs') %>
    <div class="container mx-auto p-6">
      <div class="flex flex-col md:flex-row justify-between space-y-6 md:space-y-0 md:space-x-6">
        <% if (user.is_member === false) { %>
        <div class="w-full md:w-1/2 bg-white p-8 rounded-lg shadow-md">
          <h2 class="text-3xl font-bold mb-4 text-center text-gray-800">
            Become a Member
          </h2>
          <p class="text-center text-gray-600 mb-6">
            Join the club to create and edit your own messages.
          </p>
          <form
            action="/get-membership"
            method="POST"
          >
            <div class="mb-6">
              <label for="membership" class="block text-gray-700 font-semibold mb-2"
                >Enter the Secret Code (your username):</label
              >
              <input
                type="text"
                name="membership"
                placeholder="Enter your username"
                class="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                required
              />
            </div>
            <button
              type="submit"
              class="w-full bg-blue-500 text-white py-3 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
            >
              Submit
            </button>
          </form>
        </div>
        <% } %>

        <% if (user.is_admin === false) { %>
        <div class="w-full md:w-1/2 bg-white p-8 rounded-lg shadow-md">
          <h2 class="text-3xl font-bold mb-4 text-center text-gray-800">
            Become an Admin
          </h2>
          <p class="text-center text-gray-600 mb-6">
            Gain administrative access to moderate the website.
          </p>
          <form
            action="/get-admin"
            method="POST"
          >
            <div class="mb-6">
              <label for="adminship" class="block text-gray-700 font-semibold mb-2"
                >Enter the Secret Code:</label
              >
              <input
                type="text"
                name="adminship"
                placeholder="Enter Secret Code"
                class="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                required
              />
            </div>
            <button
              type="submit"
              class="w-full bg-blue-500 text-white py-3 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
            >
              Submit
            </button>
          </form>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
